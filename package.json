{
  "name": "agri-tech-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run --prefix p1 dev",
    "build": "npm run --prefix p1 build",
    "start": "npm run --prefix p1 start"
  }
}
